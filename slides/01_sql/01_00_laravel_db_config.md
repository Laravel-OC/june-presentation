The Laravel way
---------------
```php
// connections
$connections = [
    "mysql" => [
        "driver"    => "mysql",
        "host"      => "localhost",
        "database"  => $dbname,
        "username"  => $dbuser,
        "password"  => $dbpass,
        "charset"   => "utf8",
        "collation" => "utf8_unicode_ci",
        "prefix"    => "",
    ],
];
```
